from setuptools import setup, find_packages

setup(name='MOTChallengeEvalKit',
      packages=['MOTChallengeEvalKit'],
      package_dir={'':'src'},
      version='0.0.1',
      install_requires=[],)
